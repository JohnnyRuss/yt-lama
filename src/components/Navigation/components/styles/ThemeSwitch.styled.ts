import styled from "styled-components";

export const ThemeSwitch = styled.button`
  display: flex;
  align-items: center;
  gap: 0.5rem;
  text-transform: capitalize;
  padding: 0.5rem;

  svg {
    font-size: 2rem;
  }
`;
